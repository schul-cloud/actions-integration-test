name: 'Integration Tests'
description: 'An GitHub Action to execute a full Schul-Cloud integration tests'
runs:
  using: 'docker'
  image: 'Dockerfile'
services:
  mongodb:
    image: mongo:4.2
    ports:
      - 27017:27017